---
title: "UR-10: long sessions stop at the 2 GiB heap cap"
ref: docs/upstream/runtime/heap-maximum
description: Two players stopped after three hours with the WebAssembly heap at exactly 2,147,483,648 bytes. The cap is compiled in, and Emscripten supports 4 GB on wasm32.
icon: lucide:cpu
updated: 2026-08-04
navigation:
  title: Heap maximum (UR-10)
---

Long play sessions end with an assertion abort. At the abort, the WebAssembly
heap stands at exactly 2,147,483,648 bytes — 2^31, the client's compiled-in
maximum.

**Fix:** low effort for the build flag; unknown for a leak investigation,
which depends on the growth shape. Medium blast radius — a larger address
space changes allocator behaviour and memory use on small machines, so test on
a machine with 8 GB.

::warning{title="Our evidence is not complete yet"}
We report this request with less confidence than the others. We have two crash
sessions and the compiled-in cap, but we don't yet know the growth shape: a
steady leak, an accumulation per zone, or a ratchet at map load.

The instrumentation that answers this shipped on 2026-08-03. We'll update this
page when the next crash bundle arrives; we're publishing now so the request
is on the table.
::

## What we measured

Two players, the same evening, both on client build 38797, both on an M1 Pro
with 16 GB:

| Session | Length at abort | Reason kind | Peak heap backing |
| --- | --- | --- | --- |
| `5eb4a64e` | 3 h 18 m | assertion | 2,147,483,648 |
| `9ae56272` | 2 h 23 m | assertion | 2,147,483,648 |

Both sessions stopped within six minutes of each other while the players
played together.

## The cap is compiled in

The client's glue states the cap and the reason for it:

```js
var getHeapMax = () =>
    // Stay one Wasm page short of 4GB: ...
    2147483648;
```

We read this from the artifact on disk, `Gw.jspi.js`, that the client itself
downloaded.

The build has assertions enabled, so `assert()` calls `abort()` with a text
reason, and the abort reason our classifier sees is reliable. The two machines
reported different assertion fingerprints — which points to exhaustion rather
than one specific defect: at the cap, whichever allocation fails first raises
its own assertion.

## What the client could do

The comment in the glue says the cap exists to stay clear of the 4 GB
wraparound. Emscripten supports a 4 GB heap on wasm32 with
`-sMAXIMUM_MEMORY=4GB`.

We're asking you to consider that flag — not claiming it removes the cause. It
moves the limit and buys a long session more time.

::info{title="A larger heap is not a substitute for the real cause"}
If the client leaks, a 4 GB cap doubles the time to the abort; it doesn't
remove the abort. This is a measurement, not a diagnosis — the growth shape
decides which fix is right.
::

## Acceptance criteria

- A three-hour session with several map changes does not reach the heap
  maximum.
- If you raise the cap, `getHeapMax()` returns the new value, and the client
  does not fault at the old boundary.
- The client keeps a clear abort message when it does reach the maximum.

## What we built instead of guessing

We considered a heap walker and a transform around `malloc` and `free`, and
rejected both. A leak and fragmentation need the same treatment from our side:
evidence for you, and a failure the player survives. So we shipped:

- A heap-growth event, so the growth staircase is visible per session.
- The heap size on abort, plus peak gauges.
- A technical-details view for the player, with the abort text.
- A warning at 1.75 GiB and at 1.875 GiB, with a reload at a safe moment.

The warning lets the player pick the moment of the reload. It doesn't prevent
the cause.

## How to reproduce

We can't give a short reproduction — the abort needs a long session. Our best
description:

::steps{mode="numbered"}
### Play a long session

Play for two to four hours. Change maps often. Play with other players.

### Watch the heap

Read `getHeapMax()` and the current heap size at intervals.

### Observe the abort

The client aborts with an assertion when the heap reaches the maximum.
::

## Our workaround today

We warn the player at 1.75 GiB and at 1.875 GiB, then offer a reload, syncing
the file system first where possible. A fix retires the warning and the reload
path.
