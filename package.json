{
  "name": "guild-wars",
  "productName": "Guild Wars",
  "version": "2026.7.0-beta.1",
  "private": true,
  "packageManager": "pnpm@11.13.1",
  "type": "module",
  "description": "Guild Wars for macOS",
  "license": "GPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/Mat4m0/gwonmac.git"
  },
  "bugs": {
    "url": "https://github.com/Mat4m0/gwonmac/issues"
  },
  "main": "build/main/main.js",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "postinstall": "install-electron",
    "build": "node scripts/build.mjs",
    "dev": "pnpm build && electron-forge start",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.renderer.json && tsc -p tsconfig.tests.json",
    "lint": "eslint .",
    "check:links": "node scripts/check-markdown-links.mjs",
    "test:unit": "node --import ./tests/ts-hook.mjs --experimental-strip-types --test tests/unit/*.ts",
    "test:integration": "node --test tests/integration/*.mjs",
    "test:electron": "playwright test --config=tests/electron/playwright.config.mjs",
    "test:packaged": "node tests/packaged-smoke.mjs && node tests/packaged-enhancement-runtime.mjs",
    "test:policy": "node --test tests/policy/*.mjs",
    "test:release": "node --test tests/release/*.mjs",
    "enhancements:live": "pnpm build && node scripts/enhancements-live.mjs",
    "enhancements:doctor": "pnpm build && node build/tools/enhancement-doctor.js",
    "enhancements:recertify": "pnpm build && node build/tools/enhancement-recertify.js",
    "enhancements:visual": "pnpm build && node scripts/enhancements-visual.mjs",
    "enhancements:transform": "pnpm build && node build/tools/enhancement-transform.js",
    "template:recertify": "pnpm build && node build/tools/template-save-recertify.js",
    "test:website": "pnpm --filter gw-website typecheck && pnpm --filter gw-website generate && pnpm --filter gw-website build && node tests/website-smoke.mjs",
    "check": "pnpm typecheck && pnpm lint && pnpm check:links && pnpm test:unit && pnpm test:policy",
    "test": "pnpm build && pnpm test:unit && pnpm test:integration && pnpm test:electron",
    "package": "pnpm build && node scripts/clean-output.mjs && electron-forge package",
    "make": "pnpm build && node scripts/clean-output.mjs && electron-forge make",
    "diagnostics:validate": "tsc && node build/tools/diagnostics/validate.js",
    "diagnostics:summarize": "tsc && node build/tools/diagnostics/summarize.js",
    "diagnostics:compare": "tsc && node build/tools/diagnostics/compare.js",
    "diagnostics:attribute-stalls": "tsc && node build/tools/diagnostics/attribute-stalls.js",
    "diagnostics:attribute-frames": "tsc && node build/tools/diagnostics/attribute-frames.js",
    "verify": "pnpm lint && pnpm check:links && pnpm test && pnpm test:policy && pnpm test:release && pnpm package && pnpm test:packaged"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.11.2",
    "@electron-forge/maker-zip": "^7.11.2",
    "@electron/asar": "^3.4.1",
    "@electron/fuses": "^2.1.3",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.13.3",
    "@typescript-eslint/eslint-plugin": "^8.43.0",
    "@typescript-eslint/parser": "^8.43.0",
    "electron": "^43.2.0",
    "eslint": "^10.7.0",
    "eslint-plugin-vue": "^10.10.0",
    "playwright": "^1.55.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.43.0",
    "vue-eslint-parser": "^10.4.1"
  }
}
